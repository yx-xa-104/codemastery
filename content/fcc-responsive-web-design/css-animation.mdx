---
title: "CSS Animation & Transitions"
description: "Tạo hiệu ứng chuyển động với CSS"
difficulty: "beginner"
language: "css"
---

# CSS Animation & Transitions

## Transitions — Chuyển tiếp

```css
.button {
  background: #3498db;
  color: white;
  padding: 10px 24px;
  border: none;
  border-radius: 6px;
  transition: all 0.3s ease;
}
.button:hover {
  background: #2980b9;
  transform: scale(1.05);
}
```

## Keyframe Animations

```css
@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.card {
  animation: fadeInUp 0.6s ease forwards;
}

@keyframes pulse {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.05); }
}

.badge { animation: pulse 2s ease infinite; }
```

## Timing Functions
| Giá trị | Hiệu ứng |
|---------|----------|
| `ease` | Chậm-nhanh-chậm (mặc định) |
| `ease-in` | Bắt đầu chậm |
| `ease-out` | Kết thúc chậm |
| `linear` | Tốc độ đều |
| `cubic-bezier()` | Tùy chỉnh |
